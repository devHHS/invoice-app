# W04 — 예외 처리와 마무리

`goal`: `docker compose up` 하나로 전체가 뜬다
`scope`: 전역 예외 처리(`@RestControllerAdvice`), 검색, Dockerfile, README
`note`: `InvoiceNotFoundException` 정의는 W03 토요일로 앞당겨 이미 완료됨
`out_of_scope`: Angular, 테스트, Security (다음 달)

## 일정

`InvoiceNotFoundException` 정의(원래 월요일 몫)는 W03 토요일로 앞당겨 완료했다 — 아래 표는 그만큼 하루씩 당겨서 재배치했다. 상세는 `docs/logs/W03.md` 참고.

| 요일 | 할 일 | 성공 기준 |
|---|---|---|
| 월 | `@RestControllerAdvice` 전역 처리 | 404 + JSON 에러 응답 |
| 화 | Validation 실패도 같은 응답 형태로 통일 | 400 응답 형식 일치 |
| 수 | 최소 커밋 — TODO 정리 | — |
| 목 | **Python 트랙** — 클래스로 리팩터링 | — |
| 금 | 백엔드 `Dockerfile` + compose 통합 + 검색 (`findByStoreNameContaining`) | 한 번에 전체 실행 |
| 토 | 여유일 — 금요일 작업 마무리 또는 버퍼 | — |
| 일 | **월간 회고 + README 완성** | — |

## 이해 체크

- 예외를 Controller에서 잡는 것과 `@RestControllerAdvice`로 잡는 것의 차이는?
- 쿼리 메서드 이름만 썼는데 왜 SQL이 만들어지는가?
- 왜 애플리케이션까지 Docker로 감싸는가? 로컬 실행과 뭐가 다른가?

## README에 반드시 들어갈 것

1. 프로젝트가 뭘 하는지 3줄
2. 실행 방법 (`docker compose up` 한 줄로 끝나야 함)
3. API 목록 (표)
4. 아키텍처 다이어그램 (손그림 사진도 무방)
5. 배운 것 / 앞으로 할 것

## MONTH 1 완료 조건

- [ ] `docker compose up` 하나로 API가 뜬다
- [ ] CRUD 5개 + 검색이 동작한다
- [ ] 에러 응답 형식이 통일되어 있다
- [ ] **요청이 Controller → Service → Repository → DB로 가는 흐름을 종이에 그릴 수 있다**
- [ ] 28일 연속 커밋

> 마지막 항목이 이 달의 전부다. 기능 개수는 중요하지 않다.

## 다음

W05부터 도메인 기준 패키지 재구성(모듈러 모놀리스). 이 계획의 핵심 구간이다.
