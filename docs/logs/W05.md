# W05 세션 요약

## Phase 5 — 백엔드 컨테이너화 (레슨 0016)

**한 일**
- `Dockerfile` 멀티스테이지 빌드 작성: 빌드 스테이지(`maven:3.9-eclipse-temurin-21`)에서 jar 생성 → 실행 스테이지(`eclipse-temurin:21-jre`)로 jar만 복사
- IntelliJ가 자동 생성한 기본 `Dockerfile`(ubuntu 베이스 + `top -b` 플레이스홀더)이 프로젝트와 무관한 범용 스캐폴드라는 것 확인 후 전부 교체
- `docker-compose.yml`에 `app` 서비스 추가 — `build: .`, `depends_on: db`, `ports: 8080:8080`, `SPRING_DATASOURCE_URL` 환경변수로 DB 주소를 `db:5432`로 덮어씀(로컬 실행용 `application.yml`은 그대로 두고 컨테이너 실행 시에만 값을 바꾸는 방식)
- `docker compose up --build`로 DB+앱 함께 기동 성공, 로그에서 실제 `jdbc:postgresql://db:5432/invoice-db` 접속 확인, `GET /invoices` 정상 응답 확인
- `docs/CURRICULUM.md` Phase 5의 Dockerfile 항목 + Month 1 완료 조건("`docker compose up` 하나로 API가 뜬다") 체크 완료

**질문/맥락 확장**
- "왜 이걸 하는지 모르겠다"는 질문에서 시작해 엔터프라이즈 컨테이너화 이유, 멀티스테이지 Dockerfile vs Cloud Native Buildpacks vs 레이어드 jar 비교까지 조사 후 설명 — 레이어드 jar 최적화는 오늘 스코프(새 개념 2개로 이미 충분)에서 제외하고 `NOTES.md` 백로그로 저장(합의)
- 사내에서 본 "Artifactory에서 jar 받아 AWS로 배포"가 오늘 배운 것과 같은지 질문 — Artifactory가 jar 저장소이자 Docker 레지스트리 역할도 겸한다는 점, 사내 파이프라인이 전통 방식(jar + EC2)인지 컨테이너 방식(이미지 + ECS)인지 구분하는 기준(CI 로그에 `docker build`/`push`가 있는지)을 안내

**막혔던 것과 원인**
- 특별히 막힌 지점 없음 — 빌드 스테이지/실행 스테이지/compose 값 전부 힌트(빈칸 + 설명 주석)만으로 스스로 정확히 채움

**작업 방식 관련 확인**
- 이미 완성한 코드(Dockerfile)에는 "설명 주석"(문서화 목적)을, 아직 안 쓴 부분(compose)에는 "빈칸 + 설명 주석"(직접 채우게)을 선호한다는 것 확인 — `NOTES.md`에 기록, 다음 레슨부터 기본 스캐폴딩 방식으로 채택

**다음에 이어질 것**
- `learning-records/0009`에 기록. Phase 5 잔여: TODO 정리(최소 커밋), README 완성, 월간 회고

## Month 1 회고 (레슨 0017)

**한 일**
- TODO 정리(코드 전체 확인, 남은 TODO/FIXME 없음)와 README 완성은 이전 최소 커밋 세션에서 처리 완료된 상태로 확인
- 누적 이해 체크 13문항(W01~W04 전체) 구두로 전부 답변 — 13문항 모두 정확, Q13(Docker/실행 환경 재현성)은 `learning-records/0010`에 기록된 내용을 스스로 그대로 재구성
- POST 요청 흐름(Controller→Service→Repository→DB)을 스스로 그려봄 — 처음엔 "Service가 저장 전에 응답을 먼저 만드는" 순서로 잘못 그렸다가, "id는 언제 정해지는가" 질문 하나로 스스로 순서(저장 → id 확보 → 응답 변환) 정정
- 이 대화를 `reference/0008`에 반영 — 다이어그램을 W02(DTO 도입 전) 버전에서 현재 코드(`toEntity`/`toResponse`, 실제 타입) 기준으로 갱신하고, 텍스트 6단계 설명 + "저장 전엔 id가 없다" 사이드노트 추가
- `requests.http`의 5개 요청(POST/GET전체/GET단건/PUT/DELETE) + 검색을 curl로 재확인 — 전부 정상 동작, 삭제 후 조회 시 통일된 `{"message", "status"}` 404 응답 확인
- `git log`로 28일 연속 커밋 확인 — 빠진 날짜(8/6, 8/13, 8/20)가 전부 목요일(Python 트랙, 별도 저장소)이라 이 저장소 기준으로는 연속성 유지로 판단
- `docs/CURRICULUM.md` Month 1 완료 조건 5개 + Phase 5 "월간 회고" 항목 전부 체크, `CLAUDE.md`의 `current_phase`를 "Month 1 완료 · Month 2 시작 대기"로 갱신

**막혔던 것과 원인**
- 요청 흐름을 처음 그릴 때 "저장 → 응답 변환" 순서를 "응답 변환 → 저장"처럼 헷갈리게 서술 — id가 DB에서 생성되는 시점을 명확히 안 짚어서 생긴 순서 착오, 질문 하나로 바로 스스로 수정
- (내 실수) `requests.http` curl 재현 중 한글 쿼리 파라미터를 URL 인코딩 없이 보내 400 발생 — 앱 버그가 아니라 curl 사용 실수였음을 재확인 후 `--data-urlencode`로 재검증

**Month 1 완료**
- [x] `docker compose up` 하나로 API가 뜬다
- [x] CRUD 5개 + 검색이 동작한다
- [x] 에러 응답 형식이 통일되어 있다
- [x] 요청 흐름을 종이(대화)에 그릴 수 있다
- [x] 28일 연속 커밋

**다음에 이어질 것**
- `learning-records/0011`에 기록. Month 2 — 도메인 기준 패키지 재구성(모듈러 모놀리스) 시작. `docs/CURRICULUM.md`의 Month 2 상세 항목은 아직 미채움, 다음 세션 시작 시 같이 채우기
